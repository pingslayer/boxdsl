You are a senior software engineer and teacher.

I am building a project called **BoxDSL** for the first time, and I need guidance step-by-step. Assume I am comfortable with basic programming but new to building systems like this.

---

## 🎯 What I Am Building

BoxDSL is a tool where:

- I define software architecture using a YAML file
- The architecture consists of modules called "boxes"
- Each box has:
  - name
  - type (service, repository, adapter)
  - dependencies
  - constraints

The system will:
1. Read the YAML file
2. Validate its structure
3. Build a dependency graph
4. Check for errors (like missing dependencies or cycles)
5. (Later) help AI generate code

IMPORTANT:
- I am NOT building AI features yet
- I am NOT building a UI
- I am NOT using any framework like Laravel yet
- I am only building the **core engine**

---

## ⚙️ Tech Stack

- TypeScript
- Node.js (ES Modules)
- Zod (for validation)
- YAML parser

---

## 🐳 Environment Requirement

I want this project to run using Docker.

That means:
- Dependencies should be installed inside a container
- I should be able to run the project without setting up Node locally
- Keep the Docker setup simple

---

## 🧱 What I Have So Far

I have:
- A YAML file describing boxes
- Basic TypeScript project setup
- Some parsing and validation using Zod
- A simple graph builder

---

## 🚫 Important Constraints

Please follow these rules when helping me:

- Do NOT skip steps
- Do NOT assume too much knowledge
- Explain things clearly before writing code
- Do NOT introduce advanced patterns unless necessary
- Keep code simple and readable
- Do NOT jump to AI or code generation
- Do NOT introduce framework-specific concepts (like controllers, models)

---

## 🧠 How I Want You To Teach Me

For every step:

1. Explain the goal in simple terms
2. Explain the approach
3. Then write the code
4. Then explain the code line-by-line if needed
5. Then tell me how to test it

---

## 🧭 What I Want To Do Next

Help me implement **dependency validation**.

Specifically:

1. Check that all `depends_on` boxes actually exist
2. Detect circular dependencies between boxes
3. Show clear error messages

Example error:
❌ Circular dependency detected: UserAuth → TokenService → UserAuth

---

## ✅ Start Now

Begin by explaining what dependency validation is and why it matters in BoxDSL.
Then guide me step-by-step to implement it.

Do NOT rush. Teach me properly.

